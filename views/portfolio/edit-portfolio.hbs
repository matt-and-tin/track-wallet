<h2>Edit portfolio: {{title}}</h2>
<div >
<form action="/portfolio/{{_id}}/edit" method="post">
  <h3><label> Title:
    <input type="text" name="title" value="{{title}}" required/>
  </label></h3>
</div>
  <h3><label> Coins:
    <br/>
    <div >
    <select name="coin" multiple>
      {{#each coin}}
        <option name="coin" value="{{this._id}}">{{this.name}}</option>
      {{/each}}
    </select>
    <br/>
  </label></h3>
  </div>
  <h4>Hold down the Ctrl (windows) / 
          Command (Mac) button to select multiple options.</h4>
  <br/>

  <button class="button">Save changes</button>
</form>